package in.at.interfacedemo

/*
- interface defines a list of methods that needs to be implemented
but does not define the methods implementation
- defines a contract hat a a class needs to conform to
- interface needs to be declared by using interface keyword
- in class definition the "implements" keyword hast to be used
to implement the interface
- interface is pure abstract class


 */

interface Vehicle {

    def seats()

    def wheels()

    def gears()

    def acOption()

}